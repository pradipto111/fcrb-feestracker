/**
 * Feature flags for temporarily disabling heavy features (e.g. to reduce server CPU).
 * Set to false to re-enable dashboard/analytics when ready.
 */
export const DISABLE_HEAVY_ANALYTICS = true;
